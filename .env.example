TARIFF_API_URL=https://www.trade-tariff.service.gov.uk
# Secret token sent to the backend in X-Mcp-Token — must match waf_mcp_secret_token in Terraform
MCP_SECRET_TOKEN=
# Cognito user pool that issues bearer tokens. Not needed for local development.
COGNITO_USER_POOL_ID=
